[pytest]
# Collect only the real suite. The backend root holds ad-hoc probe scripts
# (e.g. test_gdelt_fetch.py) that hit live APIs at import time; they match
# pytest's default `test_*.py` discovery and would otherwise abort collection
# when an upstream provider rate-limits.
testpaths = tests
